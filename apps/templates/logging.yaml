apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: logging
  namespace: default
spec:
  project: default
  source:
    repoURL: https://github.com/Jcobzajac/GitOps
    targetRevision: HEAD
    path: apps/charts/logging
  destination:
    server: https://kubernetes.default.svc
    namespace: default
  syncPolicy:
    automated:
      prune: True
      selfHeal: True
    syncOptions:
      - CreateNamespace=true

